# M3 R3 复验记录(2026-08-25)

基准:683d56b(R2 修复波 7ec9d01 + 动效批)/ 复验执行于 1b91289(fmt 收尾后)。

**与惯例的偏离声明**:本轮为单路复验(fable-5 主会话 + 前端/后端两个并行核验
agent,前端实跑、后端静态)。R2 的双路完整报告与 codex 索引存档于中断会话所在
机器(当前不可达),codex 腿待该机恢复后可补验;终审双票前需用户裁决是否以
单路终审替代。另一约束如实记录:执行容器网络策略拦截 ort-sys 预编译二进制
(cdn.pyke.io),**本容器无法运行任何 cargo 编译/测试**,后端行为级判定依赖
CI 三平台矩阵(PR #11 run #130 已证 Lint fmt/clippy/cargo test、E2E、
mac/win Build 全绿)。

## 前端腿(实跑 + 静态)——判定:可收口

- `pnpm test` **505/505 全绿**(与 683d56b 宣称精确一致),`pnpm build`(tsc)过。
- R2 前端四项(mode 契约/faces 用户面/FinalCutPanel 三道闸+DeliveryStatus
  共用序号/mismatch 类型)+ threshold 注释 + 动效守卫:**六项全部落地**,
  测试抽查均有实质失败路径(陈旧覆盖测试真模拟慢响应竞态;弹簧曲线逐采样点
  断言无过冲;keyframes/transition 属性白名单为真解析非 grep)。
- 备注两条(不阻塞):FinalCutPanel 自身响应序号闸无独立乱序测试(纵深防御,
  in-flight 守卫排除单实例乱序,跨写者乱序由 delivery 测试锁死);守卫测试
  实为 6 条,多于宣称的 5。

## 后端腿(静态核验)——代码修复宣称基本属实,测试网有缺口

- **R2 P0 7/7、P1 后端 18/18 已落地**;P2 9/10(A1-A14 台账映射空头,见下)。
- 测试计数自洽:`#[test]` 152→173(+21),与宣称 148→169(+21)一致。
- **核验报告的复核修正**(agent 误报,经 fixture 亲验推翻):
  `dest_symlinked_middle_dir_is_refused` 的 `!outside/100MSDCF` 断言并非死断言
  ——make_card 确实创建 `DCIM/100MSDCF/IMG_*.JPG`,闸回退时 create_dir_all
  会经链接在 outside 下产出 100MSDCF,断言为活。
- **属实缺口**:
  1. `manifest_rel_escape_is_refused_by_engine` 无效:逃逸源文件不存在,闸回退
     时 File::open 一样失败,三断言恒真;且落点断言检查的路径与
     `dest.join("../…")` 实际解析位置不符(R2 点名的恒真机理同型)。
  2. 精选/交付 mtime 端到端测试在 macOS 恒真(生产路径 fs::copy 克隆时间戳),
     判别力仅由 CI Linux/Windows 腿提供——平台依赖需显式声明。
  3. 约 13 个修复点零接线测试:P0-4 归档祖先链接闸、P0-1 resume 合并处闸、
     P0-3 manifest/journal/缩略图三写入点、归档 hwenc-fallback、faces None、
     既有产物 ffprobe 验真、取消假进度、nv12 验收分支、finalcut/delivery-status
     闸、特征缓存键、特征目录读错分支、auto_proxy 意图链(R2 成对承诺明列
     未兑现)、P0-7 EXIF 方向无样张测试。
  4. 集成断言弱两处:delivery dup「active≤1」时序依赖;cancel_job/
     list_remote_activity 为形状级断言,行为回退不可检出。
- 部分属实的宣称:P0-7「decode_oriented 单一入口」——分析路径实为内联复制同
  逻辑(为区分 Missing/Failed),两路方向语义一致,行为层面成立;CI e2e job 在
  PR 事件仍持 contents:write(GitHub 不支持按事件区分,维持声明边界)。

## R3-F1 批(本轮已落地,入 PR #11)

- 修逃逸测试:真源文件 + 按 `dest.join("../…")` 真实解析位置断言(缺口 1)。
- manifest/journal/缩略图三处 `.ocard` 闸接线回归测试(unix,缺口 3 部分)。
- 精选/交付 mtime 测试的平台依赖声明注释(缺口 2,声明路线)。
- 成片校验 E2E 冒烟 `e2e/specs/finalcut.e2e.mjs`(R1 P2 账面项兑现)。
- ORT download-binaries 制品声明边界(Cargo.toml,R2 P2 台账项)。
- CI concurrency:main 排队不取消(修复「落地 commit 无完整绿章」的流程洞)。

## R3-F1 变异实测(2026-08-25,完整网络容器——降级项补齐)

前文对测试有效性的判定均为静态推演;本节在具备完整网络的新会话容器
(Linux,rustup stable 1.98.0,Tauri v2 系统依赖 + ffmpeg sidecar +
ort 预编译库均可正常拉取)以 R2 同标准补做删闸实测,基准 `b4b23af`。

**本地基线**:`cargo test --lib` 共 173 测:root 身份 170 过 / 2 失败 /
1 ignored(analysis 基准)。两个失败(`recursive_scan_surfaces_unreadable_dirs`、
`empty_trash_failed_delete_keeps_row_and_degrades`)靠 chmod 权限位制造
「不可读/删不掉」,root 无视权限位属环境假象——以非 root 用户运行同一
测试二进制两者均过;CI runner 非 root,run #133 Lint 腿同 head 实证绿。
即本地基线等效全绿。

逐个变异 → 只跑靶测试 → 恢复;全部完成后全量重跑确认基线恢复、工作树干净:

| # | 变异(闸回退) | 靶测试 | 结果 |
|---|---|---|---|
| M1 | manifest.rs `save`:`ensure_dir_within` → 裸 `create_dir_all` | `save_refuses_symlinked_state_dir` | **红 ✓** |
| M2 | journal.rs `append`:同型回退 | `append_refuses_symlinked_state_dir` | **红 ✓** |
| M3 | media.rs `write_jpeg`:同型回退 | `thumb_write_refuses_symlinked_state_dir` | **红 ✓** |
| M4a | copy.rs `copy_one`:仅删第一层 `is_safe_rel` | `manifest_rel_escape_is_refused_by_engine` | 绿(第二层落地闸接住,逃逸未落盘——纵深防御属实,不变量测试按设计不红) |
| M4b | copy.rs `copy_one`:两层全删(`is_safe_rel` + 根链接检查/`ensure_dir_within` → 裸 `create_dir_all`) | 同上 | **红 ✓** |
| M5 | copy.rs 落位后 `preserve_times_counted` 删除 | `copy_preserves_source_mtime_end_to_end` | **红 ✓**(Linux 实机首证,此前仅 CI 腿理论判别) |
| M6 | sorting.rs 精选复制 `preserve_times_counted` 删除 | `curate_copies_and_original_stays` | **红 ✓** |
| M7 | packaging.rs `deliver_one` `preserve_times_counted` 删除 | `delivery_preserves_source_mtime` | **红 ✓** |

结论:R3-F1 五个新测试的有效性宣称全部经删闸实测证实;三处 mtime 测试
在 Linux 的判别力由 M5-M7 实证(macOS 恒真的平台声明维持不变)。
R3 复验相对 R2 的「变异实测缺席」降级项就此补齐。

## R3-F2 待办(下一批)

1. ~~P0-4 归档祖先链接闸接线测试~~ **已落地**:命令层构造链接祖先输出根,
   经真实 handler 拒绝;删 canonicalize 复核块实测红(M9)。
2. ~~P0-1 resume 合并处闸接线测试~~ **已落地**:命令层构造被篡改 manifest,
   整单拒绝且报文点名篡改;删 planned 校验块实测红(M8)。
   附带修复:闸提到卷重解析**之前**——原顺序下清单被篡改时先报
   「源卷未挂载」,用户被误导去插卡;fail-closed 应更早更诚实。
3. ~~auto_proxy 意图链挂网测试~~ **已落地**:意图→计数持久化→真实作业
   管理器出转码作业;三次未整批完成放弃+防重放+可见告警;
   删计数持久化/放弃分支实测均红(M10a/M10b)。
4. ~~EXIF 方向样张测试~~ **已落地**:手工拼 APP1 构造 Orientation=6 样张
   (样张自证:EXIF 可读出 + 摆正前后 dhash 必不同),断言索引路径
   (decode_oriented)与分析路径(analyze_one 内联摆正)两路 dhash 一致;
   ALGO_VERSION≥2 编译期断言 + 特征记录版本一致;删分析路径摆正实测红(M11)。
5. 其余零测试点(部分落地,余项如实声明):
   - **已落地**:finalcut/delivery-status 闸族——`.ocard` 链接替换时
     set/get 均拒、状态不出项目(删写闸/读闸实测均红,M12a/M12b);
     特征目录读错分支——`.ocard/analysis` 不可读必须上浮 read_err
     (改回静默空表实测红,M15)。
   - **已落地(完整网络会话追加,真实链路行为级)**:代理转码端到端
     (真 ffmpeg 转出产物→重跑幂等且**既有产物 ffprobe 验真**才计完成→
     坏产物如实报失败绝不采信,「存在即成功」回退实测红 M16)+
     **hwenc-fallback 告警**行为级(无硬编容器里软编回退必须可见)+
     **YuNet 真推理**(仓内 SHA 钉死模型 + ort 运行时在单测进程实跑,
     检测器在场时 faces 必为数字;模型解析链断开实测红 M17)。
     sidecar 缺失时测试如实跳过并可见声明(CI 在 cargo test 前已拉取)。
   - **余项**:faces Err→None 行为级(需可注入失败的检测器)、nv12 验收
     分支(需真 VAAPI 硬件)、取消假进度(需长任务取消窗口)。如实后置。
6. ~~集成断言加强~~ **已落地**:cancel_job 行为级(排队作业取消必须真进
   cancelled 终态,取消失效实测红 M13a);dup 拒绝确定性化(活跃作业在场
   必拒、终态后放行,删 has_active 闸实测红 M13b);remote_activity 折叠
   正例(他机 copy/transcode started 上榜、completed 折叠、本机不上榜;
   删折叠/删本机过滤实测均红 M14a/M14b)。

## 本地全链路实证(2026-08-25,完整网络会话)

CI 依赖的全部验证面在本容器逐一实跑,不再有"仅 CI 可证"项
(三平台矩阵与 Windows/macOS 腿仍由 CI 提供,属平台覆盖而非验证缺口):

- `cargo test --lib`:180 过 / 2 假红(chmod 权限型,root 容器无判别力,
  非 root 运行同一二进制均过)/ 1 ignored(bench)。
- `pnpm test`:505/505。
- release 基准 `bench_single_24mp`:0.378s/张,4 核外推千张 126s
  (预算 300s)——CI 性能回归步骤据此接入 Lint 腿。
- E2E(xvfb + tauri-driver 2.0.6 + debug app,与 CI 同命令):
  **4/4 spec 全过**(smoke 3 用例、sorting 6、transcode 2、finalcut
  成片校验),31s。
- 真实链路行为级:真 ffmpeg 代理端到端(验真/幂等/坏产物拒采信/
  hwenc-fallback 告警)、真 YuNet + ort 推理(faces 数字断言)。

## Blocked(需外部输入)

> 2026-08-25 更新:A1-A14 源报告已从中断会话恢复并归档,逐项映射见
> convergence 文档「A1-A14 台账」——本节第一条 Blocked 已解除;
> 「F2 继续」状态同日随全部 F2 项落地而结清(见上文勾销标记)。

- A1-A14 台账逐项映射:源报告在中断会话机器 scratchpad,凭空补写即造假。
- 真实 1000×24MP SLA 报告与 YuNet 行为级验收:待用户提供真实素材。

## Dependabot 处置(评审侧建议,合并动作留给用户)

#10 vite 8 可合(自身 CI 全绿 + worktree 实测 PR head 429/429、并 main 后
505/505);#8 sha2 0.11 可合零适配(唯一调用点 `yunet.rs` 便捷接口不受破坏性
变化影响;其 run 的 transcode E2E 一次「代理未落盘」判 flake,建议 rerun 拿绿);
#9 actions/cache v6 可合(红灯继承自 main fmt,待 main 绿后 rebase)。
顺序:#11 → #10 → #8 → #9。

## 处置

R3-F1 已随 PR #11 交 CI 验证;F2 批继续;完成后进入终审双票
(gpt-5.6-sol max + fable-5;codex 腿恢复或用户裁决单路替代)。

---

# 终审(双票)· fable-5 票

**基准**:f1a3bf1(A1-A14 台账归档后的 main)。**判定:通过**(与 gpt-5.6-sol 票合议后生效)。

核查记录(本票的验证义务,全部亲手执行):

1. **本地全量**:cargo test 182/0(1 ignored bench)、pnpm test 505/505、
   pnpm build 0、clippy -D warnings 0。R3 文档所称容器内 2 例 chmod 假红在
   本机(非 root)全过,判定成立。
2. **云批次代码审阅**:683d56b..main 全部 25 文件逐一过目——产线改动仅
   fmt/测试新增/env 进程锁/resume 闸前移(闸前移属改进:清单坏了先直说,
   不误导用户插卡),零语义削弱。
3. **变异宣称抽验**(不信宣称,亲手复跑):M8(删 resume 篡改闸)→
   resume_rejects_tampered_manifest_through_real_handler 红 ✓;M16(既有产物
   改回存在即成功)→ proxy_transcode_end_to_end_with_real_ffmpeg 红 ✓;
   恢复后全绿、工作树干净。
4. **E2E 面**:4 spec(smoke/sorting/transcode/finalcut)在仓;CI main 最新
   run 全绿;e2e-shots 截图管道 CJK 渲染正常(实图核验)。
5. **A1-A14**:源报告已从中断会话恢复归档,14 项逐一映射闭合(A11 带裁决)。
6. **UI/动效目视验收**:灰阶层次改版(用户已认可)+ apple-design 动效批
   在本地 preview 实拍验收——屏切换方向一致、工作台⇄预览同体形变、
   进度/徽标脉冲、reduced-motion 三道闸;录屏已交付用户。
7. **声明边界复核**(全部可接受、均有文字记录):Linux btime 不可设;
   Windows uncached read 回退;Windows 集成测试门(上游);faces Err 注入/
   nv12 真硬件/取消窗口三项行为级后置;macOS release 签名后字节等价改
   codesign 身份链合围;ORT download-binaries 已声明;真实 1000×24MP SLA 与
   YuNet 召回行为级验收依赖用户素材(收口报告向用户征集)。

遗留交接(P2 级,不阻塞放行):终审 codex 票报告中列出的 P2 项并入 M3 后
待办;跨机分类重放、libraw 全解码、GPU EP 维持顺延。

---

# 终审(双票)· gpt-5.6-sol 票(第一轮):不通过

报告归档:archives/2026-08-25-m3-final-codex-vote1.md。9 条放行前必修
(项目根统一 canonical 锚 / resume 哈希重验+祖先闸 / 归档闸先于副作用 /
既有产物完整验证 / 时间戳采集顺序与失败可见 / 错误即空集清零 /
FeatureRecord 模型身份+faces 自愈 / 转码出口审计+intent 作用域保证 /
账面修正)+ 4 P1。双票不齐,M3 不收口,开 R4 修复波(fable-5 执刀),
完成后 codex 复票 + fable-5 复核。fable-5 首轮票记录保留作核查存证,
其「通过」判定随双票制自动失效。

---

# 终审 · gpt-5.6-sol 第二票(不通过)与 R5 修复波

第二票报告归档:archives/2026-08-25-m3-final-codex-vote2.md(9 条阻塞 +
7 条最小必修;9 项复核中第 3 项「归档闸先于副作用」判闭环)。
连败反思(纪律触发):两轮意见同族、可执行、全部命中自家铁律——判定为
评审收敛而非漂移,按最小必修清单开 R5(fable-5 执刀):

1. resume 一次性裁决:file_done 增源侧(非链接+canonical+xxh3 重验,封同大小
   源篡改漏拷)与目的地根/祖先 canonical 闸;新增 file_done_light 供 UI 预统计,
   消除双重全量哈希;src_meta 采集再前移到 pre_existing 源哈希之前。
2. 状态读逐文件闸:manifest::list(整目录闸+链接清单计 skipped)、journal
   逐文件拒链接、features 逐文件拒链接+目录项错误显式计数;flow-hints 的
   flatten/unwrap_or 清零(条目错误聚合告警)。
3. 转码来源指纹:产物旁写 `<名>.src.json`(srcRel+xxh3(rel+size+mtime));
   既有产物先验指纹再全属性校验;归档 None 高度语义改「几何必须与源一致」+
   全路径宽高比核对;retranscode 连 sidecar 一起删;E2E 目录断言适配。
4. 模型身份进裁决:faces_rank(当前模型>旧模型>无)统一缓存命中与冲突合并;
   旧模型 Some 不再命中、合并输给当前模型。
5. 取消原子化收口:request_cancel 读态/置标志/queued 终态全程持快照锁,
   与 finish 互斥;guard 获取失败统一走 finish(Err);transition_from 死码删除。
6. intent 三腿释放:claim(创建前原子查重)→bind(job)→ 出口 guard/排队取消
   (cancel_job 按 job)/重启;guard Drop 先落 transcode_failed 审计再放 intent,
   disarm 移到终态审计写入之后。
7. 交付时间戳:预先采集的 meta_fs(先于 EXIF 半天判定)直通 deliver_one;
   preserve_times 覆盖 atime/created 测试补齐。
8. 视频缩略图:真解码验真(入口/落位后/AlreadyExists 三处),坏缓存删除后
   原子重建。

测试:180→200(R4 15 条口径修正:13 个新 #[test] + 真 ffmpeg 扩展轮,本轮
再 +7 并含第五轮「同时长他源→来源指纹拒绝」)。全量绿、clippy -D warnings 0。
完成后 codex 第三票 + fable-5 复核。
