# UX 波三双路评审收敛记录(2026-08-26)

## 范围

用户反馈第三波(基准 `32d6df0..9731f1d`,收敛 `08e721c`):
提交后失败/降级统一走 toast(11 处内联横幅拆除)、toast 竖条圆角裁剪、
级别色 --notice-* 统一、macOS 原生滚动条回归、项目用卡清单
(x/y 真分母:套用登记表模板/随时编辑/拷卡自动并入)。

## 评审配置

- Claude 路:opus,报告见 `archives/2026-08-26-ux3-opus-review.md`
- codex 路:gpt-5.6-sol `max`,报告见 `archives/2026-08-26-ux3-codex-review.md`

## 收敛结果(全部修复)

两路撞车(最高置信):
- **toast 在被会话门 inert 的 .main 里**(codex 定 P0/opus 定 P1):
  z=100 只赢了视觉,inert 吞掉命中与无障碍树——换操作人失败的唯一
  反馈点不了也读不到。修:toast host 出 .main 挂 Shell 层 + fixed;
  结构测试钉死。
- **set_project_cards 审计写失败仍返回成功**:append_audit 改返回
  Written/Outboxed/Lost,配置类事件非 Written 一律 Err。
- **保存后同屏数字打架**:面板 save 成功后 reload()。
- **降级折算成假数字**:登记表不可用→x/y 报未知(字段缺席)而非 0;
  坏 cardIds 载荷跳过并告警;journal 半损计数入 warnings。

opus 独家:已删登记卡锁死清单(set 只校验新增+死 id 剔除可见+
live_roster 过滤+空单=未配置);bindStale 迁错类回归内联;清单外拷卡
在 UI 消失(详情并列「共完成 N 次」);NOTICE_TITLES 19 个 code 补齐;
Trash 级别回归 warning+排障文案;Sorting 双报、Delivery/FinalCut 未迁移。

codex 独家:面板加载期写入口未封(空快照覆盖未知清单)→ 全封+重试;
同名旧卡卷名兜底 find 任取 → 多命中改冲突不匹配+新登记标签唯一;
A 列条/文字分母混排;PathField toast 丢字段上下文。

## 声明边界

- PROJECT_CARD_USED 自动并入为 best-effort:写失败进 outbox,
  有 audit-outbox 警告,不阻断拷卡本身。
- 多机同时编辑同一项目用卡清单:整体替换 last-write-wins
  (fold 按 (ts,file,idx) 确定);并发 used 排在 set 之后仍并入。
  merge-safe 的 per-card 事件留待有真实多机编辑需求时再上。
- journal 时序依赖各机时钟基本同步(NAS 工作站场景成立);
  时钟漂移可能让刚移出的卡被旧 used 事件加回。
- uid 冲突说明在 resolve/自动并入路径不逐项目重复上报,
  由 list_volumes 的 card-match-conflict 统一出声。

## 终态

前端 647 / Rust 211 全绿;clippy/tsc/build 干净;CI 待绿即收口。
